diff --git a/examples/deploy-watch.yaml b/examples/deploy-watch.yaml
index 7197793..34c275e 100644
--- a/examples/deploy-watch.yaml
+++ b/examples/deploy-watch.yaml
@@ -1,3 +1,7 @@
+# Poll-until pattern: repeatedly check a deployment until it reaches a terminal state.
+# Completion is decided externally: the status endpoint must report `green`.
+# Use this when external state changes over time and needs repeated checks or remediation.
+# Prefer a one-pass task when one run can finish the work without waiting or rechecking.
 loopspec: "0.1"
 id: deploy-watch
 meta:
