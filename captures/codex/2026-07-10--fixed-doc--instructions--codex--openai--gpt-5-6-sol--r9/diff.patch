diff --git a/examples/deploy-watch.yaml b/examples/deploy-watch.yaml
index 7197793..f9e0865 100644
--- a/examples/deploy-watch.yaml
+++ b/examples/deploy-watch.yaml
@@ -1,3 +1,8 @@
+# Poll-until pattern: repeatedly check an external deployment until its status
+# endpoint reports `green`, which is the condition that completes this loop.
+# Use this pattern when completion depends on asynchronous state changing in an
+# external system. Use a one-pass task when one run can finish the work without
+# waiting for an external state transition.
 loopspec: "0.1"
 id: deploy-watch
 meta:
