diff --git a/packages/core/test/validate.test.ts b/packages/core/test/validate.test.ts
index a00c238..4b827df 100644
--- a/packages/core/test/validate.test.ts
+++ b/packages/core/test/validate.test.ts
@@ -1,5 +1,5 @@
 import { describe, expect, it } from "vitest";
-import { getBlueprint, listBlueprints, loadSpecFromYaml, processRaw } from "../src/index.js";
+import { formatValidation, getBlueprint, listBlueprints, loadSpecFromYaml, processRaw } from "../src/index.js";
 
 function codes(raw: unknown): string[] {
   const r = processRaw(raw);
@@ -59,6 +59,28 @@ describe("validator hard gates", () => {
     expect(codes(raw)).toContain("bad-ref");
   });
 
+  it("formats an undeclared step condition reference", () => {
+    const raw = {
+      ...validBase,
+      body: [
+        {
+          id: "w",
+          kind: "agent",
+          harness: "claude-code",
+          prompt: "do",
+          when: "${state.nope == 1}",
+          on_done: { set: { done: true } },
+        },
+      ],
+    };
+    const result = processRaw(raw);
+    const output = formatValidation(result.validation!);
+
+    expect(output).toContain("✗ invalid");
+    expect(output).toContain("[bad-ref]");
+    expect(output).toContain("body[0]:w.when");
+  });
+
   it("requires explicit caps for a self-assess termination signal", () => {
     const { caps, ...noCaps } = validBase;
     void caps;
